# --- Runtime ---
fastapi==0.116.0
uvicorn[standard]==0.35.0        # ASGI 서버
python-multipart==0.0.20         # 파일 업로드
pydantic==2.11.7                 # 데이터 모델
sentence-transformers==5.0.0     # E5 임베딩
qdrant-client==1.14.3            # 벡터 DB
unstructured==0.18.3             # 문서 추출
Pillow==11.3.0                   # 이미지 처리
requests==2.32.4                 # HTTP 클라이언트 (Ollama API용)

streamlit==1.46.1                # 웹 UI 프레임워크

# --- OCR (택1) ---
# ① Tesseract 로컬 OCR
pytesseract==0.3.13

# --- Worker / 배치 ---
celery==5.5.3
redis==6.2.0                     # 기본 브로커
numpy==1.26.4
